# Session Memory — 2026-05-20

## Summary
Worked on grouping kanban cards by project within columns.

## Changes made
- **`src/components/KanbanColumn.vue`** — Reworked the `filteredCards` computed so
  cards are grouped by `projectId`. All cards belonging to the same project now
  render adjacent to each other in every column (ToDo, In Progress, Testing, etc.).
  - Project groups keep their **first-appearance order** (the project of the
    topmost card stays on top), so the layout is stable.
  - Cards with **no project** sort to the bottom of the column.
  - Grouping applies whether or not project/label filters are active.

## Known follow-up / open items
- Drag-and-drop still reorders against the real underlying `column.cards` array.
  Dragging works, but after a drop the view re-snaps cards into their project
  groups — the grouped order is **not** persisted to Firestore. If persisted
  grouping is desired later, `moveCard` / `persistColumns` in `stores/board.js`
  would need to physically regroup the stored array.

## Context notes
- App was renamed from "Vibe Board" to **Flowmancer**.
- Repo rule: never run git commands.
